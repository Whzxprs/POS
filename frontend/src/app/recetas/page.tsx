import { RecipeBuilder } from '../../features/recetas/RecipeBuilder';

export default function RecetasPage() {
  return (
    <div style={{ height: '100%', overflowY: 'auto' }}>
      <RecipeBuilder />
    </div>
  );
}
